global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']   # Prometheus scrapes itself

  - job_name: 'node'
    static_configs:
      - targets: ['node_exporter:9100']   # Prometheus scrapes Node Exporter by container name

